{
  "name": "project-tracker",
  "version": "1.0.0",
  "description": "Full-stack project tracker application with React frontend and GraphQL backend",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm start --prefix backend\" \"npm run preview --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "Thang Truong",
  "license": "ISC"
}

